#include <stdio.h>
#include <unistd.h>
#include <windows.h>

int main()
{
    int p = 1;
    if (p != p != p)
    {
        printf("true");
    }
    else
    {
        printf("False");
    }
    return 0;
}